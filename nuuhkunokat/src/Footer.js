import React from 'react';
import Button from './Button';

const Footer = () => {
  return (
    <div class = "footer">
      <Button icon={brokenheart}/>
      <Button icon={heart}/>
    </div>
  );
};

export default Footer;
